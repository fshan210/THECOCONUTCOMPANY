{
  "name": "@dotco/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts",
    "synth": "cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.177.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "aws-cdk": "^2.177.0",
    "esbuild": "^0.24.2",
    "tsx": "^4.20.6",
    "typescript": "^5.5.3"
  }
}
